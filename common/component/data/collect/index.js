const {
  byConvention
} = require('./name-conventions')
const {
  Loggable
} = require('../../../loggable')
const {
  Model
} = require('./model')
const {
  Declarations
} = require('./declarations')
const {
  Imports
} = require('./imports')
const {
  Tag
} = require('./tag')

module.exports = {
  byConvention,
  Loggable,
  Model,
  Declarations,
  Imports,
  Tag
}
