const {
  TemplateWriter,
  createTemplateWriter
} = require('./template')
const {
  createRegistrator,
  Registrator
} = require('./registrator')

module.exports = {
  createTemplateWriter,
  createRegistrator,
  Registrator,
  TemplateWriter
}
