const {
  Logger
} = require('./logger')

const {
  BaseGenerator
} = require('./base')

const {
  BaseComponentGenerator
} = require('./component')

const {
  BaseBoilerplateGenerator
} = require('./boilerplate')

module.exports = {
  Logger,
  BaseGenerator,
  BaseComponentGenerator,
  BaseBoilerplateGenerator
}
