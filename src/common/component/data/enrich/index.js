const {
  Declarations,
  createDeclarations
} = require('./declarations')
const {
  Imports,
  createImports
} = require('./imports')

module.exports = {
  Declarations,
  createDeclarations,
  Imports,
  createImports
}
