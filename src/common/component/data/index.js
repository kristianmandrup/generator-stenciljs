const collect = require('./collect')
const prepare = require('./prepare')
const {
  DataModel,
  createDataModel
} = require('./data-model')

module.exports = {
  collect,
  prepare,
  DataModel,
  createDataModel
}
